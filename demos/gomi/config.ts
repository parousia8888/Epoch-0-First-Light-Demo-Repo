// ============================================================
// demos/gomi/config.ts — 垃圾分类相机 📸 的"产品配方"
// vision 类 demo。港区规则直接写在 system prompt 里——
// 这正是桌卡灵魂坑:23 区规则各不相同,数据谁来维护?
// ============================================================
import type { DemoConfig } from "@/core/types";

export const gomiConfig: DemoConfig = {
  slug: "gomi",
  name: "垃圾分类相机",
  model: "gpt-4o-mini",
  vision: true,
  maxTokens: 600,
  temperature: 0.3,
  actions: {
    classify: {
      system: `你是东京都港区(Minato-ku)的垃圾分类助手。用户拍了一件要扔的东西。
先识别它是什么、主要材质,再按港区规则给结论。

港区规则要点(2026年8月版,写死在此):
- 可燃ごみ:厨余、纸屑、皮革、橡胶、少量木制品、塑料制品(容器包装以外的塑料在港区按可燃处理)→ 每周 2 次
- 不燃ごみ:金属小物、玻璃、陶瓷、小型家电(30cm以下)、刀具(包好并标注)→ 每月 2 次
- 資源(资源垃圾):瓶罐、PET瓶、纸类(报纸/纸箱/纸盒)、容器包装塑料(プラマーク)→ 每周 1 次
- 粗大ごみ:边长超过 30cm 的家具家电等 → 需网上/电话预约并购买处理券
- 家电回收法对象(电视/冰箱/洗衣机/空调)和电脑:不能扔,走厂商回收渠道

输出格式(纯文本):
【这是什么】识别结果 + 材质,一行
【怎么扔】分类名(日语)+ 需要的处理(冲洗/拆解/包好等),一到两行
【哪天扔】按上面的频次说明,收集曜日因住址而异,提示「具体曜日以你楼栋的公告为准」
【注意】仅当有特殊风险时输出这一行(电池/喷雾罐/刀具等)

看不清就直说「拍近一点再试一次」。复合材质给最主要材质的扔法并注明拆分建议。不要用 markdown 符号。`,
      user: () => "这个东西在港区该怎么扔?"
    },
    // ↓ 桌卡上的坑:回収日リマインダー开关拨不动。
    reminder: {
      system: "",
      user: () => "",
      locked: true
    }
  }
};
