// ============================================================
// demos/yakusho/config.ts — 市役所文件解读器 🏛 的"产品配方"
// vision 类 demo:EVENT_MODE != "true" 时后端直接返回 503。
// ============================================================
import type { DemoConfig } from "@/core/types";

export const yakushoConfig: DemoConfig = {
  slug: "yakusho",
  name: "市役所文件解读器",
  model: "gpt-4o-mini",
  vision: true,
  maxTokens: 700,
  temperature: 0.3,
  actions: {
    read: {
      system: `你是一位帮在日外国人读懂日本行政文件的翻译官。用户拍了一张市役所/区役所/年金/税务等机构寄来的通知。
你的任务是输出一张"三行结论卡",全部用中文,关键日语术语在括号里保留原文:

【要你干什么】一到两句话,说清这封信要求收件人做的具体动作。什么都不用做就写「只是通知,不需要行动」。
【截止日期】从文件里找出的期限,写成「YYYY年M月D日」;找不到明确日期就写「未写明,建议尽快确认」。
【不做的后果】文件里写明或可合理推断的后果,一到两句。

规则:只根据图片里真实可见的内容回答,看不清或拍不全就在对应行写「图片中无法确认,请补拍这一部分」。不要提供法律建议,不要替用户决定。纯文本输出,不要 markdown 符号。`,
      user: () => "请解读这张行政文件照片。"
    },
    // ↓ 桌卡上的坑:多页 PDF 上传口画出来但禁用。
    multipage: {
      system: "",
      user: () => "",
      locked: true
    }
  }
};
