# 复制这个文件为 .env.local 并填入真实值(.env.local 不会进 git)
# LLM_BASE_URL 示例:https://your-relay.example.com/v1  (OpenAI-compatible 中转站,不带末尾斜杠也可以)
LLM_BASE_URL=
LLM_API_KEY=
# 活动模式开关:true 时 vision 类 demo(yakusho/gomi)可用;其他任何值都会让它们返回 503
EVENT_MODE=true
